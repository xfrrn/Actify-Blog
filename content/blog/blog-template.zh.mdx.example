---
# 中文博客模板：.mdx.example 不会被读取或公开显示。
# 复制为 content/blog/my-note.zh.mdx；英文译文复制为 my-note.en.mdx。
# 两份文件的 my-note 必须相同，才能共用 /blog/my-note 并跟随语言切换。
# 可用 .md 或 .mdx；文件名主体只使用小写英文、数字和连字符。

# 【必填】当前语言的标题和摘要。
title: "我的第一篇记录"
description: "用一句话说明这篇文章讲什么。"

# 【必填】发布日期，YYYY-MM-DD。建议两种语言使用同一天，以保持排序一致。
date: "2026-09-15"

# 【可选】语言从文件名 .zh / .en 推断，也可显式填写，但必须与后缀一致。
# 不带语言后缀的旧文件，默认中文；无需手写 slug，它由文件名自动生成。
language: zh

# 【可选】自由命名的分类，不关联项目。省略或留空表示未分类。
# 为保持筛选一致，同篇文章的两个语言版本请使用相同分类名。
category: "学习记录"

# 【可选，默认 []】标签，可按当前语言填写。
tags: ["笔记"]

# 【可选】封面路径（对应 public/ 下文件）或完整 HTTPS 图片地址。
# cover: "/blog/my-note.png"
# 【可选】当前语言的图片说明，供屏幕阅读器和图片无法显示时使用。
# coverAlt: "这张封面展示的内容"

# 【可选】最后更新日期，YYYY-MM-DD；省略时使用发布日期。
# updatedAt: "2026-09-16"
# 【可选】作者名；省略时使用网站作者 Actify。
# author: "Actify"

# 【可选，默认 false】本模板设为草稿，准备好再改为 false。
# 每个语言版本独立控制发布；只有一个版本公开时，会回退显示该版本。
draft: true
---

这是一篇用于复制的示例，不代表已发布的经历。请替换为你的真实记录。

## 我做了什么

在这里写正文。标题、摘要写在上面的文件头，正文从二级标题开始。

## 学到了什么

支持 **加粗**、[链接](https://example.com)、图片、列表、表格和代码块。

```ts title="example.ts"
const message = "你好";
console.log(message);
```

## 下一步

- 写下准备继续做的事情。
- 配对英文版本也要填写自己的标题、摘要、正文和图片说明。

{/* content（正文）、mdx（编译结果）、toc（目录）、readingMinutes（阅读时间）由系统生成，无需填写。
旧字段 publishedAt、summary、image 仍兼容，分别对应 date、description、cover；新文章使用新字段即可。
发布前删除示例内容、检查链接，最后将 draft 改为 false。 */}
