---
# 项目模板：本文件以 .md.example 结尾，不会被网站读取或显示。
# 使用方法：复制本文件，重命名为 my-tool.md，放在当前 content/projects/ 目录。
# 文件名只能用小写英文、数字、连字符；它会自动成为 slug，无需填写 slug 字段。
# 填完真实内容并确认可以公开后，将下面的 draft 改为 false。

# 【必填】项目名称，中英文页面共用。建议使用项目的正式名称。
name: "My Tool"

# 【必填】两种语言的项目简介，显示在作品卡片中。
# en 和 zh 都需要填写。支持简单 Markdown，例如 **加粗**。
# >- 表示可以分行书写，显示时合并为一段文字。
description:
  en: >-
    Describe what your tool does and who it helps.
  zh: >-
    介绍这个工具做什么、适合谁使用。

# 【可选，默认 []】技术标签，显示在卡片底部。没有就填 [] 或删除此字段。
technologies: ["TypeScript", "Next.js"]

# 【可选】项目时间，自由填写的文字，两种语言共用；不确定就省略。
# dates: "2026.09 — Present"

# 【可选】封面图片。文件放在 public/projects/，这里写从 / 开始的路径。
# 也可填写完整的 HTTPS 图片地址。封面比例为 16:9。
# 没有图片或加载失败时，会自动显示项目名称文字封面。
# image: "/projects/my-tool.png"

# 【可选】展示视频的地址，也支持 public/ 下的路径或完整 HTTPS 地址。
# 视频会静音、自动、循环播放；同时填写 image 和 video 时，优先展示 video。
# video: "/projects/my-tool.mp4"

# 【可选】源码仓库链接。未公开源码就省略，不要保留示例地址。
# github: "https://github.com/your-name/your-repo"

# 【可选】在线体验/官网链接，也可以是站内路径（例如 /）。
# 卡片点击优先打开 demo，没有 demo 时打开 github；两者都没有则不跳转。
# demo: "https://example.com"

# 【可选】项目状态，只能选下面三种之一，页面会自动翻译状态标签：
# Building = 开发中；Live = 已上线；Archived = 已归档。
# 没有需要公开的状态时，直接省略。
# status: "Building"

# 【可选，默认 false】true 表示在首页精选作品区展示。
# 非草稿项目无论是否精选，都会显示在 /projects 作品汇总页。
featured: false

# 【可选，默认 100】排序整数，越小越靠前；相同时按文件名排序。
# 建议使用 10、20、30 等间隔，方便以后在中间插入新项目。
order: 100

# 【可选，默认 false】true 为草稿，不显示在首页或作品汇总页。
# 模板特意设为 true，填写完成再改为 false 发布。
draft: true
---

此处是 Markdown 正文，对应自动读取的 content 字段，无需在文件头中另填。
当前作品仅展示卡片，正文不会显示，也不会生成项目详情页。
可以在这里保存自己的项目备忘；卡片中要显示的简介请填写上面的 description。

新建检查：

- 重命名为唯一的英文文件名，例如 my-tool.md。
- 填写项目名称、中英文简介，删除不需要的可选字段。
- 需要使用的可选字段，删除行首的 # 并填入真实内容。
- 图片、视频文件已放到对应路径，源码和体验链接可正常打开。
- 按需设置 featured 和 order，准备公开时将 draft 改为 false。
- pnpm dev 可自动读取修改；构建预览和线上站点需要重新构建。
